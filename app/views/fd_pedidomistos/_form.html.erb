<%= form_for(@fd_pedidomisto) do |f| %>
  <% if @fd_pedidomisto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fd_pedidomisto.errors.count, "error") %> prohibited this fd_pedidomisto from being saved:</h2>

      <ul>
      <% @fd_pedidomisto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fd_produto_id %><br>
    <%= f.text_field :fd_produto_id %>
  </div>
  <div class="field">
    <%= f.label :fd_itempedidos_id %><br>
    <%= f.text_field :fd_itempedidos_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
