<%= form_for(@fd_itensproduto) do |f| %>
  <% if @fd_itensproduto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fd_itensproduto.errors.count, "error") %> prohibited this fd_itensproduto from being saved:</h2>

      <ul>
      <% @fd_itensproduto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fd_produto_id %><br>
    <%= f.text_field :fd_produto_id %>
  </div>
  <div class="field">
    <%= f.label :fd_item_id %><br>
    <%= f.text_field :fd_item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
