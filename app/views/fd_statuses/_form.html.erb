<%= form_for(@fd_status) do |f| %>
  <% if @fd_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fd_status.errors.count, "error") %> prohibited this fd_status from being saved:</h2>

      <ul>
      <% @fd_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome_status %><br>
    <%= f.text_field :nome_status %>
  </div>
  <div class="field">
    <%= f.label :desc_status %><br>
    <%= f.text_field :desc_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
